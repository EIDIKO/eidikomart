# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build eidiko-monorepo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t eidiko-monorepo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system eidiko-monorepo && \
          adduser --system -G eidiko-monorepo eidiko-monorepo

COPY dist/apps/eidiko-monorepo eidiko-monorepo
RUN chown -R eidiko-monorepo:eidiko-monorepo .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix eidiko-monorepo --omit=dev -f install

CMD [ "node", "eidiko-monorepo" ]
